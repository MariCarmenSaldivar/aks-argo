apiVersion: v1
kind: Secret
metadata:
  name: mydumper-config
  namespace: backup-jobs
type: Opaque
stringData:
  mydumper.cnf: |
    [mydumper]
    host = mysql.example.internal
    port = 3306
    user = backup_user
    password = replace-me
    database = appdb
